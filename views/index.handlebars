<div class="jumbotron">
    <h4 class="display-4">Bem Vindo ao Macuxi Deluxe</h4>
    <p class="lead">Aqui você encontrará diversas histórias sobre um grupo de amigos.</p>
    <hr class="my-3">
    <p>Esse blog foi feito com o intuito de trazer diversão a quem interessa.</p>
    
</div>

<hr>

<h3>
    Postagens recentes:
</h3>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
          
        <h4>{{titulo}}</h4>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
          <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
          <small>Data de publicação: {{data}}</small>
        
    </div>
</div>
{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}